# Shell scripts must keep LF - CRLF breaks the shebang on Linux hosts
# (Railway/Render), which is where this bot actually runs.
*.sh text eol=lf

# Batch files need CRLF to be safe on Windows.
*.bat text eol=crlf
